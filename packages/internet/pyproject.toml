[project]
name = "mitorag-web"
version = "0.1.0"
description = "Scientific web API clients for MitoRAG Phase 5."
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mitorag_web"]
