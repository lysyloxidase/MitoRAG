[project]
name = "mitorag-retrieval"
version = "0.1.0"
description = "Hybrid retrieval package boundary for MitoRAG."
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mitorag_retrieval"]
